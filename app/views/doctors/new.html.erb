<%= simple_form_for [@doctor] do |f| %>
  <%= f.input :first_name %>
  <%= f.input :last_name %>
  <%= f.input :user_id, :as => :hidden, :input_html => { :value => current_user.id } %>
  <%= f.button :submit %>
<% end %>
